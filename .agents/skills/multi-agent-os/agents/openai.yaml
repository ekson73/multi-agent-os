interface:
  display_name: "Multi Agent Os"
  short_description: "Repo-specific patterns and workflows for multi-agent-os"
  default_prompt: "Use the multi-agent-os repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true