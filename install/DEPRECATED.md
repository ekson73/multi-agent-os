# DEPRECATED

This directory is deprecated as of v1.0.0.

## Migration

Installation is now handled via Claude Code plugin system:

```bash
# Install as plugin
claude plugins install /path/to/multi-agent-os

# Or use directly
claude --plugin-dir /path/to/multi-agent-os
```

## Scheduled Removal

This directory will be removed in v2.0.0.

---

*Deprecated: 2026-01-07*
