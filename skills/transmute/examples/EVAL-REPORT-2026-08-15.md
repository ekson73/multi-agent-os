# EVAL-REPORT — transmute (skill + command) — 2026-08-15
- Baseline: v0.1.1 · Golden cases: 5 (smoke-set — real invocation traces rounds n+1→n+4 + inspection)
- Evaluator: agentic-tool-evaluator v1.1.0 (behavioral, with/without control approximated by pre-router vs post-router traces)

## Scores (0–5)

| Case | Trigger | TaskCompl | ToolCorr | Effic | ScopeFit | Regression |
|------|---------|-----------|----------|-------|----------|------------|
| C1 placeholder-empty input class (4 real occurrences: n+1–n+4) | 5 (router catches + names placeholder + emits fill-snippet since n+3) | 4 (correct STOP-ERROR; snippet DX added) | 5 | 5 (no wasted pipeline) | 5 | 0 |
| C2 invocation surface `/transmute` (commands wrapper exists, name matches) | 5 | 5 | 5 | 5 | 5 | 0 |
| C3 DRY routing (routes to siblings; no restatement — 15.5KB vs 45KB Lapidary; SSOTs cited in-line) | n/a (inspection) | 5 | 5 | 5 | 5 | 0 |

> **C3 caveat (recorded 2026-08-15, round n+5)**: this 5/5 was measured **intra-maos only**.
 Cross-harness recon (eko-engram ledger) found the vault family had independently
 evaluated the same directive-class and dropped minting a generic enhancer
 (compose-not-fork). Post-audit: **no overlap** — Alambique is the vault-side
 placement SSOT (PARA + ledger + persist-locus routing); transmute is the repo-side
 invocation router. Cross-links added in v0.1.3. Score stands.
| C4 safe-by-default (dry-run default; leak gate unconditional; worktree on git sinks) | n/a (spec inspection) | 4 (spec'd; untested on a dirty source — no real dirty-source run yet) | 4 | 5 | 5 | 0 |
| C5 real source → cast → emit (a genuine filled braindump through COMPREHEND→…→EMIT) | 5 (re-scored — see Verdict) | **5 — EXERCISED (cycle 2)** | 5 | 5 | 5 | 0 |

## Verdict: FLAG → **PASS (re-scored 2026-08-15, dogfood cycle 2)**

**C5 re-score: 2 → 5.** Full pipeline exercised on a real source (operator's
recurring `/enhance` round template): INTAKE (typed directive-template) →
COMPREHEND (9-part catalogue + key relation: the template's verb cascade IS the
transmute spec) → TRANSFORM (dedupe/session-meta strip/flag-mapping) → CAST
(prompt seat) → EMIT (path sink: **`~/.codex/prompts/transmute-round.md`**).
Deliverable independently useful. All cases ≥4 → **PASS**.

> **Evidence correction (2026-08-15, round n+8)**: this paragraph originally cited the
> sink as `prompts/transmute-round.md` — a repo-relative reading that resolves to
> nothing (`multi-agent-os/prompts/` does not exist, and the path was never committed).
> The artifact is real and was verified this round: `~/.codex/prompts/transmute-round.md`
> (3051 B, 2026-08-14, frontmatter self-identifies as *"Generated by /transmute dogfood
> cycle 2 … from 4 real invocation traces"*). It lives in the **Codex** harness prompt dir,
> not in this repo. The companion claim *"synced to akasha-codex"* is **RETRACTED as
> uncorroborated**: `~/akasha-codex` contains 4 files (`README.md`, `.gitignore`,
> `AGENTS.md`, `docs/decisions/0001-user-scope-bootstrap.md`) and no `prompts/` dir.
> The cycle stands; only its citation was wrong. A path a reader cannot resolve is
> indistinguishable from a fabricated one — which is why it is corrected rather than
> left to be re-litigated next round.

## Strengths
- The router's failure-mode design was **validated by real input**: the placeholder-empty class occurred 4× and each occurrence improved the surface (n+1 detect → n+3 snippet DX → n+4 `## Optional` template evolution acknowledged).
- Invocation-surface gate satisfied at creation (forge) AND at eval (wrapper present, name match).
- DRY discipline measurable (size ratio + SSOT citations grep-verifiable).

## Weaknesses
- ~~**C5 is the open hole**~~ — **CLOSED by cycle 2** (artifact verified 2026-08-15 round n+8, see Verdict). Superseded; struck rather than deleted so the trail of what was open, and when it closed, survives.
- **C4 is now the open hole**: safe-by-default is still `n/a (spec inspection)` — no run against a genuinely dirty source. Scored 4 on spec, never on behavior.
- With/without control is approximated (pre-router inline execution vs post-router delegation), not isolated-subagent A/B — smoke-set confidence LOW-to-MEDIUM.
- **Chained cast is unimplemented and the gap is undeclared** (round n+8): §When-to-use promises *"the operator wants MULTIPLE casts of one source"*, but `--to-type` takes one value and the `"cast"` field of the output contract is a singleton object, not an array. The recurring operator pipeline `source → cast:prompt → cast:agentic-tool` therefore requires two manual invocations. This is a spec/promise mismatch, not a bug — recorded here because an eval that scores only what was built will never surface it.

## Recommendation

**Superseded 2026-08-15 (round n+8).** The prior text read *"Hold promotion gate closed. Next:
ONE real `/transmute "<filled source>"` run (dogfood cycle 2), then re-score C5."* — that run
happened; this line simply outlived it. Kept visible because a stale recommendation sitting under
a PASS verdict is the reason this report contradicted itself for a day.

→ **Promotion gate: OPEN, and now corroborated by the counting authority** — not by prose.
Cycles 001/002 were backfilled into the SSOT ledger (`~/.claude/audit/dogfood-cycles.jsonl`)
with citable evidence; `bin/dogfood-tally transmute` reports `2 complete · GATE>=2 ELIGIBLE`.
Before this round the changelog claimed 2 cycles while the ledger held **0** — the exact
per-skill *island counter* that `skills/dogfood-ledger/SKILL.md:56` forbids. The claim was true;
it was merely unmeasurable, which under `anti-theater` is not a lesser defect.

> **⚠️ Auditability limit — declared, not glossed** (raised by CodeRabbit on PR #343, upheld):
> the ledger is **user-scope and machine-local** (`~/.claude/audit/`, by design per
> `skills/dogfood-ledger/SKILL.md:16` — "one user-scope, append-only, jq-countable ledger …
> fed by any session/project/vendor"). It is therefore **NOT auditable from a clone of this
> repository**. A reviewer without the operator's machine can verify the *upstream* evidence
> the entries cite — PRs #328/#330, and the artifact's existence — but must take the tally
> itself on trust.
> This is the same defect class this very report corrects one section above (a citation the
> reader cannot resolve), so it is recorded rather than asserted away. It is a **known
> limitation of a deliberately user-scope design**, not a defect introduced here: a
> cross-machine ledger would have to solve identity and merge, which `dogfood-ledger` did not
> set out to do. Reproduce locally with `bash bin/dogfood-tally transmute`.

→ **Next, in order:** (1) exercise **C4** on a genuinely dirty source — the one case still scored
by inspection alone; (2) decide the chained-cast gap: either implement `--to-type` as a list with
an array `cast` contract, or delete the multi-cast promise from §When-to-use. Shipping a
capability in the prose and not in the grammar is the failure mode this report exists to catch.
