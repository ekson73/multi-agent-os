# Régua v0.2 — disposições pós-piloto (2026-08-18)

> Fecha o ciclo do piloto (#354): cada critério FAIL ou PASS virou fix (#355-#360) OU disposição registrada aqui. Nada fica em limbo.

## Status final das 8 skills do piloto

| Skill | Piloto | Disposição final |
|---|---|---|
| agentic-delegation | 0/2 | ✅ FIXED v1.1.0 (#355) — judge PASS |
| decompose-abstract-to-measurable | 0/2 | ✅ FIXED v1.3.0 (#356) — judge PASS |
| quiesce | 0/3 | ✅ FIXED v0.3.0 (#357) — judge 3/3 PASS |
| postflight | 0/2 | ✅ FIXED v0.10.0 (#359) — judge PASS |
| directive-braindump-triage | 1/2 | ✅ FIXED v1.1.0 (#360) — C1 atômico, judge PASS |
| agentic-tool-forge | 2/2 | ✅ já conforme no piloto |
| morning-briefing | 0/2 | ➡️ **disposição abaixo (D1)** |
| praxis-audit | 0/2 | ➡️ **disposição abaixo (D2)** |

## D1 — morning-briefing C1/C2: o FAIL era real, e a cura é version-sync

O juiz FAILou a cópia do REPO (v1.0.0, 203 linhas). Verificação Tomé: a cópia user-scope
(`~/.agents/skills/morning-briefing`, v1.7.0) JÁ tem Pulse-callout com zero-counts +
idempotency guard — a evolução existe, só nunca foi portada de volta. **Drift repo×user-scope
confirmado e medido (1.0.0 vs 1.7.0).**

**Disposição**: o fix não é editar a cópia velha — é o **version-sync port** (user-scope v1.7.x →
repo), que exige sanitização de paths/convenções user-scope (precedente: a promoção v1.0.0
sanitizou atribuições proprietárias). PR dedicado, com ciclo de revisão próprio.
**C2 permanece válido como escrito** (zero-counts no Pulse é o que a v1.7.0 faz). **C1 migra de
mecanismo para intenção no v0.2** (per o registro de discordância do piloto): exige "qualquer gate
explícito contra re-render imediato" — não o mecanismo específico ("aviso ou degrade --quick").

Evidência do user-scope (verificável): `~/.agents/skills/morning-briefing/SKILL.md` ·
version 1.7.0 · sha256 `2beaa2d1d375a569…` · mtime 2026-08-06 17:26 (local, mutável — snapshot
registrado aqui per o padrão de evidência da régua).

## D2 — praxis-audit C1: mis-scope do autor (meu), critério migra

O critério C1 (veredito-stale → re-medir contra head) foi ancorado numa falha de contexto
PR-review, mas praxis-audit é skill de *processo* ("not the produced RESULT"). Erro do autor
da rubrica (lead), não do skill. **Disposição**: C1 migra para o conjunto de critérios de
`quiesce`/`pr-review` no v0.2 das rubricas (quiesce já cobre C3 stale-verdict — verificar
dupla cobertura no merge); praxis-audit sai do conjunto avaliado com esta nota, e ganha um
critério novo derivado de falha de processo real quando houver trace.

## Regra de ouro aprendida (entra no v0.2 da régua)

**Copiar skill entre escopos sem canal de sync = drift garantido.** O v0.2 da régua deve medir
drift repo×user-scope como invariante da Camada 0 (par de mesma skill, hash/section diff).
